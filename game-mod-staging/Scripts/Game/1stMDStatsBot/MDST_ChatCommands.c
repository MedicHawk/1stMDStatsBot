// Chat command integration is intentionally disabled in this standalone addon build.
// Hook MDST_StatsGameModeComponent.GetInstance().SendLinkCode(playerId, code)
// from a scenario-specific chat/UI addon that already depends on the required UI modules.
